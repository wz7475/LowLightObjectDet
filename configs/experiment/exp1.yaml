# @package _global_

defaults:
  - override /model: fasterrcnn
  - override /datamodule: datamodule
  - override /trainer: trainer

model:
  lr_head: 0.1

trainer:
  max_epochs: 81

datamodule:
  batch_size: 8
